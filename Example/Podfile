use_frameworks!

target 'SimpleFrameButton_Example', :exclusive => true do
  pod 'SimpleFrameButton', :path => '../'
end

target 'SimpleFrameButton_Tests', :exclusive => true do
  pod 'SimpleFrameButton', :path => '../'

  pod 'Quick', '~> 0.8'
  pod 'Nimble', '~> 3.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
